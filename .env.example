# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# 站点前缀（部署在子路径时设置，例如 /fineval；根路径留空）
NEXT_PUBLIC_BASE_PATH=

# 兼容变量（不推荐，若使用需与最终域名一致）
# BASE_URL=https://your-project-name.vercel.app